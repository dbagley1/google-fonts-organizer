import FontList from "../components/FontList";

function FontPage({ user }) {
  return (
    <div className="container">
      FontPage
      <FontList user={user} />
    </div>
  );
}

export default FontPage;
